# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# MySQL: Connection url for the database "pzhu"
spring.datasource.url = jdbc:mysql://localhost:3306/pzhu?characterEncoding=utf8

# MySQL: Username and password
spring.datasource.username = root
spring.datasource.password = cjm37934bit

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
# spring.jpa.hibernate.ddl-auto = create-drop

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect



# ===============================
# = Data - Rest
# ===============================
spring.data.rest.basePath=/api

# ===============================
# = UTF-8
# ===============================
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true
spring.http.encoding.force-request=true
spring.http.encoding.force-response=true

# ===============================
# = Security
# ===============================
spring.security.user.name=springboot
spring.security.user.password=isawesome

# ===============================
# = 调整端口
# ===============================
server.port=8091
# ===============================
# = 邮件
# ===============================
spring.mail.host = smtp.163.com
spring.mail.username = 13550907617@163.com
spring.mail.password = 37934bit
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable = true
# 自定义的配置
mail.fromMail.address = 13550907617@163.com